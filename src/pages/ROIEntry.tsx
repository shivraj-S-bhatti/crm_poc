import React from 'react';
import { Typography } from '@mui/material';

const ROIEntry: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">New ROI Entry</Typography>
      {/* Placeholder for ROI entry form */}
    </div>
  );
};

export default ROIEntry; 