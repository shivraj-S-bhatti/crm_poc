import React from 'react';
import { Typography } from '@mui/material';

const History: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Previous ROI Entries</Typography>
      {/* Placeholder for history table */}
    </div>
  );
};

export default History; 